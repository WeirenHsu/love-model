services:
  api:
    build: .
    ports:
      - "8000:8000"
    healthcheck:
      # 用 /health 判斷「服務真的準備好了」，而不只是「容器啟動了」
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 5s
      timeout: 3s
      retries: 5

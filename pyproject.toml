[project]
name = "love-model"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.*",
    "uvicorn[standard]==0.30.*",
]

[dependency-groups]
dev = [
    "pytest==8.*",
    "pytest-cov==5.*",
    "httpx==0.27.*",
    "ruff==0.6.*",
    "pre-commit==3.*",
]
